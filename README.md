# PharaohGate 🏛️

Библиотека для создания красивых анимаций в стиле Древнего Египта в SwiftUI.

## Возможности

- 🏛️ Красивый портал храма с градиентом и анимированным лоадером
- 🦂 Анимированные скарабеи с пульсирующим эффектом
- 🔺 Пирамиды с вращающимися элементами  
- 🏜️ Фон пустыни с движущимися песчинками
- 📜 Веб-просмотрщик папируса с поддержкой жестов и обновления
- ⚱️ Универсальная система проверки доступности внешнего контента (Осирис)
- 🐕 Кастомные alert'ы с переходом в настройки (Анубис)
- 🦅 Быстрая интеграция OneSignal для push-уведомлений (Хорус)

## Установка

### Swift Package Manager

Добавьте зависимость в ваш `Package.swift`:

```swift
dependencies: [
    .package(url: "https://github.com/yourusername/PharaohGate.git", from: "1.0.0")
]
```

Или добавьте через Xcode:
1. File → Add Package Dependencies
2. Введите URL репозитория
3. Выберите версию

## Использование

### Портал Храма (Temple Portal)

```swift
import SwiftUI
import PharaohGate

struct ContentView: View {
    var body: some View {
        PharaohGate.createTemplePortal(
            gradientColors: [.blue, .purple, .pink],
            textColor: .white,
            loaderColor: .white,
            loadingText: "Loading..."
        )
    }
}
```

### Анимированный Скарабей (Animated Scarab)

```swift
PharaohGate.createAnimatedScarab(
    size: 50,
    color: .yellow,
    duration: 1.0
)
```

### Пирамида (Pyramid)

```swift
PharaohGate.createPyramid(
    elementCount: 8,
    size: 200,
    colors: [.blue, .purple, .pink]
)
```

### Пески Пустыни (Desert Sands)

```swift
PharaohGate.createDesertSands(
    sandCount: 50,
    speed: 2.0
)
```

### Веб-просмотрщик Папируса (Papyrus Display)

```swift
PharaohGate.createPapyrusDisplay(
    urlString: "https://example.com",
    allowsGestures: true,
    enableRefresh: true
)
```

#### Особенности веб-просмотрщика папируса:

- ✅ Поддержка JavaScript
- ✅ Свайпы для навигации назад/вперед
- ✅ Pull-to-refresh жесты
- ✅ Обработка дочерних окон
- ✅ Автоматическое открытие внешних ссылок
- ✅ Сохранение куки
- ✅ Современный User Agent
- ✅ Отступы от safe area
- ✅ Черный фон для лучшего отображения

### Проверка доступности контента (Osiris Checker)

```swift
// Проверяем доступность внешнего контента (суд Осириса)
let targetDate = Calendar.current.date(from: DateComponents(year: 2025, month: 3, day: 4))!
let result = PharaohGate.checkContentAvailability(
    url: "https://example.com",
    targetDate: targetDate,
    deviceCheck: true,
    timeout: 10.0,
    cacheKey: "myApp" // Уникальный ключ для кэширования
)

if result.shouldShowExternalContent {
    // Показываем WebView с внешним контентом
    PharaohGate.createPapyrusDisplay(urlString: result.finalUrl)
} else {
    // Показываем основное приложение
    MainAppView()
}
```

#### Особенности системы проверки Осириса:

- ✅ **Кэширование результатов** - проверка выполняется только один раз
- ✅ **Проверка интернета** - автоматическая проверка соединения
- ✅ **Проверка даты** - контент доступен только после указанной даты
- ✅ **Проверка устройства** - исключение iPad (опционально)
- ✅ **Проверка сервера** - валидация ответа сервера (коды 200-403)
- ✅ **Подробные логи** - отладочная информация в консоли
- ✅ **Уникальные ключи** - разные приложения не влияют друг на друга

#### Логи в консоли:

```
🔍 Начинаем проверку доступности контента для URL: https://example.com
🌐 Проверяем интернет соединение...
✅ Прошли интернет
📅 Проверяем целевую дату...
✅ Прошли дату
📱 Проверяем тип устройства...
✅ Прошли проверку устройства
🌐 Проверяем серверный код...
✅ Прошли код
🎉 Все проверки пройдены! Сохраняем результат...
```

### Кастомные Alert'ы (Anubis Alerts)

```swift
// Alert для уведомлений с переходом в настройки (призыв Анубиса)
PharaohGate.showNotificationsAlert()

// Универсальный кастомный alert (сообщение Анубиса)
PharaohGate.showCustomAlert(
    title: "Подтверждение",
    message: "Вы уверены, что хотите продолжить?",
    primaryButtonTitle: "Да",
    secondaryButtonTitle: "Нет",
    primaryAction: {
        print("Пользователь подтвердил действие")
    },
    secondaryAction: {
        print("Пользователь отменил действие")
    }
)

// Alert с подтверждением (суд Анубиса)
PharaohGate.showConfirmationAlert(
    title: "Удалить данные",
    message: "Это действие нельзя отменить",
    confirmTitle: "Удалить",
    cancelTitle: "Отмена",
    onConfirm: {
        // Удаляем данные
        print("Данные удалены")
    }
)
```

#### Особенности alert'ов Анубиса:

- ✅ **Автоматический поиск root view controller** - работает в любом месте приложения
- ✅ **Переход в настройки** - автоматическое открытие Settings.app
- ✅ **Обработка действий** - callback'и для кнопок
- ✅ **Безопасность** - проверка доступности view controller'а
- ✅ **Подробные логи** - отладочная информация в консоли

### Интеграция OneSignal (Horus Notifications)

```swift
import SwiftUI
import OneSignalFramework
import PharaohGate

@main
struct MyApp: App {
    @UIApplicationDelegateAdaptor(AppDelegate.self) var appDelegate

    var body: some Scene {
        WindowGroup { ContentView() }
    }
}

final class AppDelegate: UIResponder, UIApplicationDelegate {
    func application(
        _ application: UIApplication,
        didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?
    ) -> Bool {
        // Благословение Хоруса
        PharaohGate.initializeOneSignal(
            appId: "YOUR-ONESIGNAL-APP-ID",
            launchOptions: launchOptions
        )
        return true
    }
}
```

#### Что делает менеджер Хоруса:

- ✅ Инициализирует OneSignal и логинит пользователя с `PharaohGate.getUserID()`
- ✅ Хранит счётчик запусков, чтобы запрашивать разрешение только на первом старте
- ✅ При последующих запусках проверяет статус разрешения и показывает Alert от Анубиса
- ✅ Автоматически вызывает `OneSignal.login` после получения разрешения

> **Важно:** Добавьте `OneSignalAppID` в Info.plist, включите push capability и пропишите правильный App ID.

### Получение ID пользователя (Ra ID Generator)

```swift
// Получаем уникальный ID от бога солнца Ра
let userId = PharaohGate.getUserID()
print("User ID: \(userId)")
```

## Архитектура (Египетские Боги)

### 🌞 **PharaohGate** - Главный портал
Основной API библиотеки, предоставляющий доступ ко всем возможностям.

### ☀️ **RaIDGenerator** - Генератор ID от Ра
Бог солнца Ра дарует каждому пользователю уникальный идентификатор.

### ⚰️ **OsirisAvailabilityChecker** - Судья Осирис
Бог загробного мира проверяет доступность внешнего контента и принимает решение.

### 🐕 **AnubisAlertManager** - Вестник Анубис
Бог-проводник показывает важные сообщения и ведёт пользователя к настройкам.

### 🦅 **HorusNotificationManager** - Небесный Хорус
Бог неба управляет push-уведомлениями и благословляет их отправку.

### 🏛️ **TemplePortalView** - Портал храма
Красивый экран загрузки в стиле египетского портала.

### 🦂 **AnimatedScarabView** - Священный скарабей
Анимированный скарабей, символ возрождения и вечной жизни.

### 🔺 **PyramidView** - Великая пирамида
Вращающиеся элементы вокруг центральной оси, как звезды вокруг пирамиды.

### 🏜️ **DesertSandsView** - Пески времени
Движущиеся песчинки пустыни, символизирующие течение времени.

### 📜 **PapyrusDisplayView** - Древний папирус
Веб-просмотрщик с современными возможностями и древней эстетикой.

## Требования

- iOS 15.0+
- macOS 12.0+
- watchOS 8.0+
- tvOS 15.0+
- Swift 5.9+

## Лицензия

MIT License

---

**Сделано с ⚱️ в духе Древнего Египта**
